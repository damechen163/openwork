# Troubleshooting

Run `openwork doctor --json` first. A blocked result is expected on non-Linux development
hosts. Each failed check includes remediation. Do not paste raw environment files or
credentials into an issue.

Phase 0 does not start containers, migrate a database, or contact a model provider. If a
document suggests otherwise, file a documentation bug.
