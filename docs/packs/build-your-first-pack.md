# Build your first pack

The v1 authoring tutorial will land with Issue #8. A pack will declare its API version,
metadata, OpenWork compatibility, runtime capabilities, filesystem/network/shell permissions,
approval requirements, license, and self-tests. Until the schema is merged, do not publish
manifests claiming compatibility.
