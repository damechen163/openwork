# Installation profiles

Profiles will compose a small core with explicit optional services. No optional upstream
may silently become a v0.1 dependency. Profile manifests must use exact versions and image
digests; `latest` is forbidden.
